# Component Build Directory - mpsystem_build_build_build/CMakeLists.txt
# Generated by OBINexus Component Regeneration Protocol
cmake_minimum_required(VERSION 3.16)

project(mpsystem_build_build_build VERSION 1.0.0 LANGUAGES C)

# Import core build system utilities
include(${CMAKE_SOURCE_DIR}/cmake/ComponentSystem.cmake)

# Define component sources
file(GLOB_RECURSE COMPONENT_SOURCES 
     "${CMAKE_SOURCE_DIR}/src/core/mpsystem_build_build/*.c"
     "${CMAKE_SOURCE_DIR}/src/core/mpsystem_build_build/*.h")

# Define component dependencies
set(COMPONENT_DEPENDENCIES "")

# Register component with build orchestration system
nlink_register_component(
    NAME mpsystem_build_build
    SOURCES ${COMPONENT_SOURCES}
    DEPENDENCIES ${COMPONENT_DEPENDENCIES}
    ENABLE_STATIC TRUE
    ENABLE_SHARED FALSE
)

# Generate component verification artifacts
add_custom_target(mpsystem_build_build_verification
    COMMAND ${CMAKE_COMMAND} -E echo "Verifying component integrity: mpsystem_build_build"
    COMMAND ${CMAKE_COMMAND} -E echo "Component dependencies: ${COMPONENT_DEPENDENCIES}"
)

# Register component verification with build system
add_dependencies(mpsystem_build_build mpsystem_build_build_verification)
