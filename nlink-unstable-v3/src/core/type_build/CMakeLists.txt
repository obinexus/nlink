# type_build/CMakeLists.txt
# Generated by OBINexus Component Integration System
cmake_minimum_required(VERSION 3.16)

# Component integration with build orchestration
project(type_build VERSION 1.0.0 LANGUAGES C)

# Import build system utilities
include(${CMAKE_SOURCE_DIR}/cmake/ComponentSystem.cmake)

# Define component sources
file(GLOB_RECURSE COMPONENT_SOURCES 
     "${CMAKE_SOURCE_DIR}/src/core/type/*.c"
     "${CMAKE_SOURCE_DIR}/src/core/type/*.h")

# Define component dependencies
set(COMPONENT_DEPENDENCIES "common")

# Register component with build system
nlink_register_component(
    NAME type
    SOURCES ${COMPONENT_SOURCES}
    DEPENDENCIES ${COMPONENT_DEPENDENCIES}
    ENABLE_STATIC TRUE
    ENABLE_SHARED FALSE
)

# Verification layer
add_custom_target(type_verification
    COMMAND ${CMAKE_COMMAND} -E echo "Component integrity verified: type"
)
add_dependencies(type type_verification)
